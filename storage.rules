rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /csv-files/{fileName} {
      allow read, write: if request.auth == null; // Allows unauthenticated users to upload
    }
  }
}
